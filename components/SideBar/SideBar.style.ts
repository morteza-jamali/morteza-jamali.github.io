export const drawer: MORAJ.StyleFunction = (css) =>
  css({
    width: "240px",
    flexShrink: 0,
  });

export const drawerPaper: MORAJ.StyleFunction = (css) =>
  css({
    width: "240px",
    border: "none",
  });

export const listItem: MORAJ.StyleFunction = (css) =>
  css({
    textTransform: "uppercase",
  });
