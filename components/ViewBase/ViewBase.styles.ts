import { css } from 'glamor';
import type { IViewBaseStyleProps } from './ViewBase.types';

export const Styles = ({}: IViewBaseStyleProps) => {
  return { root: css({}) };
};

export default Styles;
