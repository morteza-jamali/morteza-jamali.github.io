import { Bare } from '@workspace/morteza-jamali/blog/components';
import type { NavbarComponent } from './navbar.types';

export const Navbar: NavbarComponent = (props) => {
  return <Bare {...props} />;
};

export default Navbar;
