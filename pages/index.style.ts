export const fixedBox: MORAJ.StyleFunction = (css) =>
  css({
    inset: 0,
    position: "fixed",
  });

export const flexBox: MORAJ.StyleFunction = (css) =>
  css({
    inset: 0,
    position: "fixed",
    display: "flex",
  });
