import React from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

const Icon: IconComponent.Function = ({ icon }) => (
  <FontAwesomeIcon icon={icon} />
);

export default Icon;
